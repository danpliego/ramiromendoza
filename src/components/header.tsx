import React from "react";
import styled from "styled-components";

export const Header: React.FC = () => {
  return <HeaderContainer>eit</HeaderContainer>;
};

const HeaderContainer = styled.div`
  padding: 1.5rem 0;
`;
